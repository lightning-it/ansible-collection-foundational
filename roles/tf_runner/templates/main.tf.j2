terraform {
  required_version = ">= 1.3.0"
}

# Generic module call; all variables are passed via terraform.tfvars.json
# and do not need to be listed here explicitly.
module "root" {
  source = "{{ tf_runner_module_source }}"
}
