---
vmware_vsphere_guest_bootstrap_module_defaults:
  hostname: "{{ vmware_vsphere_guest_bootstrap_hostname }}"
  username: "{{ vmware_vsphere_guest_bootstrap_username }}"
  password: "{{ vmware_vsphere_guest_bootstrap_password }}"
  validate_certs: "{{ vmware_vsphere_guest_bootstrap_validate_certs }}"
