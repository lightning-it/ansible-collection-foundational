---
- name: Verify ansible remote tmp syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.foundational.ansible_remote_tmp coverage stub ran
      ansible.builtin.assert:
        that:
          - ansible_remote_tmp_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
