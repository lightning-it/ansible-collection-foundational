---
- name: Verify oob redfish control syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.foundational.oob_redfish_control coverage stub ran
      ansible.builtin.assert:
        that:
          - oob_redfish_control_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
