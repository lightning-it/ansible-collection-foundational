---
bugfixes:
  - >-
    onepassword_approval and onepassword_ssh_key_import - accept their complete
    controller action argument schemas in the remote fallback stubs so a
    missing action plugin always fails with the intended explicit safety
    message instead of an unsupported-parameter error.
