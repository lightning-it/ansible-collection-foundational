---
minor_changes:
  - >-
    Add the controller-only ``onepassword_vault_co_manager`` action to verify and idempotently grant one exact
    user the fixed ``allow_viewing``, ``allow_editing``, and ``allow_managing`` permission set on one exact vault,
    with executable pinning, desktop-only authentication, one-time approval, check mode, and post-change readback.
