---
minor_changes:
  - >-
    onepassword_ssh_key_import - add a controller-only, target-confirmed path
    for importing an exact existing Ed25519 key through standard input and
    verifying it through the pinned 1Password SSH Agent.
