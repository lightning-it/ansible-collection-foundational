---
minor_changes:
  - >-
    Add the controller-local ``ansible_vault_secret_document`` action plugin for immutable, CAS-safe creation and
    validation of encrypted secret documents with Ansible's already-loaded Vault identity.
